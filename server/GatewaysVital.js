class GatewaysVital {
  constructor(id, lastSeenAt, createdAt, gateway) {
    this.id = id
    this.lastSeenAt = lastSeenAt
    this.createdAt = createdAt
    this.gateway = gateway
  }
}

module.exports = GatewaysVital
